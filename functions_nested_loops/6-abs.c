#include "main.h"
/**
 * _abs - Function that computes the absolute value
 * @al: Input a number
 * Return: Absolute value
 **/
int _abs(int al)
{
	if (al >= 0)
	{
		return (al);
	}
	else
	{
		return (al * -1);
	}
}
