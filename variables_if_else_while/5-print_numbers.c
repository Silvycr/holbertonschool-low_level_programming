#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Number
 *
 * Return: zero on success
 */
int main(void)
{
	int al;

	for (al = 0; al < 10; al++)
		printf("%d", al);
	printf("\n");

	return (0);
}
